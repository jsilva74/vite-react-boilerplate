import {
  styled,
  Backdrop as MBackdrop,
  CircularProgress as MCircularProgress,
} from '@mui/material';

export const Overlay = styled(MBackdrop)`
  z-index: 1500;
`;
export const CircularProgressIcon = styled(MCircularProgress)`
  color: #ffffff;
`;
